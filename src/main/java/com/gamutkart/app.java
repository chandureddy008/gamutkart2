package com.gamukart;

public class App {
    public static void main(String[] args) {
        int i; // Removed the colon ":" and declared i as an integer
        int j; // Removed the colon ":" and declared j as an integer

        for (i = 0; i <= 100; i++) { // Changed the semicolon to a colon
            i += 5; // Corrected the addition
            i += 10; // Corrected the addition
        }
    }
}

